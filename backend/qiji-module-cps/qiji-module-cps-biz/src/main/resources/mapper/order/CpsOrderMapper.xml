<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiji.cps.module.cps.dal.mysql.order.CpsOrderMapper">

    <!-- 统计指定日期各平台订单数据 -->
    <select id="selectDailyStatsByDate" resultType="map">
        SELECT
            platform_code,
            COUNT(*) AS order_count,
            SUM(CASE WHEN order_status NOT IN ('refunded','invalid') THEN 1 ELSE 0 END) AS new_order_count,
            COALESCE(SUM(final_price), 0) AS order_amount,
            COALESCE(SUM(commission_amount), 0) AS commission_amount,
            COALESCE(SUM(CASE WHEN order_status IN ('settled','rebate_received') THEN commission_amount ELSE 0 END), 0) AS settled_commission_amount,
            COALESCE(SUM(CASE WHEN order_status IN ('created','paid','received') THEN commission_amount ELSE 0 END), 0) AS pending_commission_amount,
            COALESCE(SUM(real_rebate), 0) AS rebate_amount,
            COUNT(DISTINCT member_id) AS active_member_count
        FROM cps_order
        WHERE DATE(create_time) = #{statDate}
          AND deleted = 0
          AND tenant_id = #{tenantId}
        GROUP BY platform_code
    </select>

    <!-- 实时看板：指定日期全平台汇总数据 -->
    <select id="selectRealtimeDashboard" resultType="map">
        SELECT
            COUNT(*) AS order_count,
            COALESCE(SUM(final_price), 0) AS order_amount,
            COALESCE(SUM(commission_amount), 0) AS commission_amount,
            COALESCE(SUM(real_rebate), 0) AS rebate_amount,
            COALESCE(SUM(commission_amount) - SUM(real_rebate), 0) AS profit_amount,
            COUNT(DISTINCT member_id) AS active_member_count
        FROM cps_order
        WHERE DATE(create_time) = #{statDate}
          AND deleted = 0
          AND tenant_id = #{tenantId}
    </select>

    <!-- 趋势统计直接读取订单表，确保历史订单未生成日汇总时仍可展示 -->
    <select id="selectTrendStatsByDateRange" resultType="map">
        SELECT
            DATE(create_time) AS stat_date,
            COUNT(*) AS order_count,
            COALESCE(SUM(commission_amount), 0) AS commission_amount,
            COALESCE(SUM(real_rebate), 0) AS rebate_amount,
            COALESCE(SUM(commission_amount), 0) - COALESCE(SUM(real_rebate), 0) AS profit_amount,
            COUNT(DISTINCT member_id) AS active_member_count
        FROM cps_order
        WHERE DATE(create_time) BETWEEN #{startDate} AND #{endDate}
          AND deleted = 0
          AND tenant_id = #{tenantId}
          AND (#{platformCode} = 'total' OR platform_code = #{platformCode})
        GROUP BY DATE(create_time)
        ORDER BY DATE(create_time)
    </select>

    <!-- 平台分布直接读取订单表，确保区间内实际订单可见 -->
    <select id="selectPlatformStatsByDateRange" resultType="map">
        SELECT
            platform_code,
            COUNT(*) AS order_count,
            COALESCE(SUM(commission_amount), 0) AS commission_amount,
            COALESCE(SUM(real_rebate), 0) AS rebate_amount,
            COALESCE(SUM(commission_amount), 0) - COALESCE(SUM(real_rebate), 0) AS profit_amount
        FROM cps_order
        WHERE DATE(create_time) BETWEEN #{startDate} AND #{endDate}
          AND deleted = 0
          AND tenant_id = #{tenantId}
        GROUP BY platform_code
        ORDER BY commission_amount DESC
    </select>

</mapper>
